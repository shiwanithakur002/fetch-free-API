# Fetch Free API - Random Joke App

This is a simple React application that fetches and displays random jokes using a free public API.

## 🔧 Features

- Fetches a random joke from a public API
- Clean and minimal React UI
- Fast and responsive

## 🚀 Getting Started

Clone the repository and install dependencies:

```bash
git clone https://github.com/shiwanithakur002/fetch-free-API.git
cd fetch-free-API
npm install
